const uniqueInOrder=function(iterable){
  let arr1=[iterable[0]];
  for (let i=1; i<iterable.length; i++){
    if (iterable[i-1]!==iterable[i]) arr1.push(iterable[i]);
  }
return (iterable.length!==0) ? arr1 : [];
}
