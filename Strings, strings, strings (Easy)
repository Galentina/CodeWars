Number.prototype.toString = function(){
  return ''+ this;
}
Boolean.prototype.toString = function(){
  return (this==true) ? 'true' : 'false';
}

Array.prototype.toString = function(){
let res=this.reduce((a,b)=> a+b+',',''); return '['+res.slice(0,res.length-1)+']'
}
