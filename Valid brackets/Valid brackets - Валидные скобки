function validParentheses(str){
if (str.length%2!==0) return false;
else {
  while(str.includes('()'))
    {str=str.replace('()','')}
}
  console.log(str);
  return (str.length===0) ? true : false;
}
