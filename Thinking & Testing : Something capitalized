function testit(s){
  let str = '';
  for (i = 0; i < s.length; i++) {
     if (s[i + 1] === ' ' || i === s.length - 1) {
        str =str+ s[i].toUpperCase();
     } else {
        str =str+ s[i];
     }
  }
  return str;
}
