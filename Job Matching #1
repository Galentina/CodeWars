function match(candidate, job) {
  if (!candidate.minSalary || !job.maxSalary) throw TypeError('No salary indication')
return (job.maxSalary>=candidate.minSalary-candidate.minSalary/10) ? true : false
}
