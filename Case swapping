function swap(str){
let str1='';
  for (let i=0; i<str.length; i++) {
    if (str[i]===str[i].toLowerCase()) str1=str1+str[i].toUpperCase();
    else str1=str1+str[i].toLowerCase(); 
  } return str1;
}
