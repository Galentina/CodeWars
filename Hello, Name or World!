function hello(name) {
  if (!name || name.length===0) return `Hello, World!`;
  let name1=name.split('').map((el,i)=> (i===0) ? el.toUpperCase() : el=el.toLowerCase()).join('');
  return `Hello, ${name1}!`;
}
