function completeSeries(arr) {
    let a=[0], b=arr.filter(el => arr.indexOf(el) !== arr.lastIndexOf(el));
   if (b.length===0){
   let i=1; 
   while(i<=Math.max(...arr)) {a.push(i); i++;}
 } return a;
}
