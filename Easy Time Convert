function timeConvert(num) { 
let hours= (Math.floor(num/60)<10) ? '0'+Math.floor(num/60) : ''+Math.floor(num/60);
let minutes=(num%60<10) ? '0'+num%60 : ''+num%60;
return (num<=0) ? "00:00": hours + ":" + minutes;
}
